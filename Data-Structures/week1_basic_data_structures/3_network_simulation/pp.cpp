#include <bits/stdc++.h>
using namespace std;


int main()
{

int S,n;
cin >> S >> n;

dequeue <pair <int,int>> process;

vector <pair <int,int>> buffer;

for(int i = 0;i<n;i++)
{
  cin >> a[i] >> b[i];
  event.insert(make_tuple(a[i], 'S', i));
  event.insert(make_tuple(b[i],'E',i+b[i]));
}
int i = 0;
for (auto e:event)
{

}

return 0;
}
